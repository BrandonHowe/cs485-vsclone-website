# AGENTS.md

## Package Manager

This project uses **bun** (not npm/yarn/pnpm). Use `bun install`, `bun run dev`, `bun add`, etc.

## Tech Stack

- Next.js (App Router) with TypeScript
- Tailwind CSS v4
- Fonts: Inter + JetBrains Mono via next/font
